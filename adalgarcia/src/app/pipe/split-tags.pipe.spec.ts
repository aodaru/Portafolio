import { SplitTagsPipe } from './split-tags.pipe';

describe('SplitTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
